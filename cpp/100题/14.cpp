#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    int maxVowels(string s, int k) {
        int result = 0, vw = 0;
        
        for(int i=0; i<=s.size(); i++)
        {
            if
        }
    }

    bool is_vw(char c)
    {
        if (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u')
        {
            return true;
        }
        return false;
    }
};